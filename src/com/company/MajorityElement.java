package com.company;import java.util.ArrayList;import java.util.Collections;import java.util.HashMap;import java.util.Iterator;import java.util.Map;/** * Created by Hoshiko on 10/16/15. */public class MajorityElement {    public static boolean  majorityElement(int[] array){        int N = array.length;        int majorityNum = N / 2;        int result = 0;        HashMap <Integer, Integer> counter = new HashMap<Integer, Integer>();        for (int i = 0 ; i < N ; i++){            counter.put(array[i],1);        }        int maxValueInMap=(Collections.max(counter.values()));  // This will return max value in the Hashmap        for (Map.Entry<Integer, Integer> entry : counter.entrySet()) {  // Itrate through hashmap            if (entry.getValue()==maxValueInMap) {                result =  entry.getKey();     // Print the key with max value            }        }        if (result > majorityNum){            return true;        } else {            return false;        }    }}